define(['underscore', 'backbone'], function (_, Backbone) {

    var global = window || {},
        recline = global.recline || {},
        recline.View = recline.View || {},
        recline.Backend = recline.Backend || {},
        recline.Model = recline.Model || {};

    return recline
});
